<h1>メッセージ一覧</h1>

<ul>
  <% @messages.each do |message| %>
    <!--link_to　表示文字列,リンク先という形
    　　リンク先として、Modelのインスタンス（レコード）を渡すと、
    　　自動的にそのインスタンスshowアクションへとリンクされる-->
    <li><%= link_to message.id,message %> : <%= message.content %></li>
  <% end %>
</ul>

<%= link_to '新規メッセージの投稿' , new_message_path %>